components:
  Pet:
    content:
      application/json:
        schema:
          $ref: '../schemas/pet.yaml#/Pet'
      application/xml:
        schema:
          $ref: '../schemas/pet.yaml#/Pet'
    description: Pet object that needs to be added to the store
    required: true